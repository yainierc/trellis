# trellis scratch — gitignored, and never referenced from a committed file (rules/README.md §3)
tmp/
.trellis/worktrees/
.trellis/active
.trellis/wave.json
.DS_Store

# local permissions, per machine — never committed
.claude/settings.local.json
